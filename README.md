# Electronic-Voting-Machine-EVM-
Arduino-based Electronic Voting Machine (EVM) prototype using push buttons, buzzer, and Serial Monitor to record votes, count votes, display results, and identify the winner.
